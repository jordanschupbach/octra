// #include <octra/c/linked_list.h>
// #include <octra/c/octra_io.h>
#include <stdio.h>

int main() {
  // printf("Allocating linked list");
  // octra_linked_list* myList = octra_linked_list_alloc();

  // int num1 = 1;
  // int num2 = 2;
  // int num3 = 3;
  // int num4 = 7;

  // octra_linked_list_push_back(myList, &num2);
  // octra_linked_list_push_back(myList, &num3);
  // octra_linked_list_push_back(myList, &num1);
  // octra_linked_list_push_front(myList, &num4);

  // int  search_data  = 2;
  // int  search_data2 = 4;
  // int* value        = (int*)octra_linked_list_find(myList, (void*)&search_data);
  // int* value2       = (int*)octra_linked_list_find(myList, (void*)&search_data2);

  // int* value3 = (int*)octra_linked_list_next(myList, myList->head);

  // printf("Linked list elmt: %d\n", value);  // BUG:? expects int?
  // printf("Linked list elmt: %d\n", value2); // BUG:? expects int?
  // printf("Linked list elmt: %d\n", value3); // BUG:? expects int?

  // // printf("Linked list elmt: %d\n", value3); // BUG:? expects int?

  // octra_linked_list_print(myList, octra_print_int);
  // printf("Linked list size: %d\n", octra_linked_list_size(myList));
  // octra_linked_list_free(myList);

  return 0;
}
