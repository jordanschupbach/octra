There is no support for rust in swig. But, i still want bindings to rust. CAn you add them using whatever binding generation is the most automatic like swig. I'd like it to have its own nix environment that the library is installed to and run repls/tests and example scripts from there. Can you add the necessary files and configuration to make this happen?


