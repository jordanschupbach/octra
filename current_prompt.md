In this repo, c++ is bound to a bunch of different languages using swig. Swig,
has some tools for generating documentation, but it is limited to just a few
languages (e.g. python, java, c#). This repo uses doxygen to generate documentation for the
c++ code. Can you make sure that the documentation is generated for all the languages that swig supports?
